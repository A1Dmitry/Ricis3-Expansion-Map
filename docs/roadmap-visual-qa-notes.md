# Roadmap visual QA notes

**Environment:** local development server, `/?view=roadmap`.

## Checked

- Страница Roadmap открывается без ошибки и не требует отдельной загрузки данных.
- Первый экран показывает четыре равноправные карточки: Explore, Verify, Challenge и Root Goal.
- У каждой карточки видны вопрос, ожидаемый результат, пример и отдельная CTA.
- Карта, задачи и методология доступны как быстрые ссылки.
- На desktop не наблюдается горизонтальная прокрутка; заголовок, четыре карточки и quick links видимы в первой области страницы.

## Next interactive checks

1. Открыть root-route и выбрать публичную цель.
2. Проверить, что выдача показывает только `scientific_task` с путём к корню.
3. Проверить empty state, return to map, URL-state и контекстный переход из node card.

## Interactive root-filter result

- После выбора `core-agi-target` Roadmap отобразила явный бейдж корня, счётчик и карточки только научных задач.
- Выдача содержит семь задач, для каждой показан объясняющий путь до корня.
- Путь с промежуточным контекстным узлом отображается корректно: `informatics-complexity → ai-authorship-provenance → core-agi-target`; промежуточный `ai-authorship-provenance` не показан как ложная карточка задачи.
- Визуально задача, краткое описание, CTA открытия и путь читаемы; длинное описание переносится и не ломает карточку.

## Follow-up checks

1. Проверить возврат в карту и сохранение URL-контекста.
2. Выполнить мобильный smoke-test.
3. Запустить полный lint/test/build после завершения UI-изменений.
