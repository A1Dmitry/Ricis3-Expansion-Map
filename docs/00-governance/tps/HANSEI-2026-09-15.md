# HANSEI — ретроспектива такта 1 (2026-09-15)

**Цикл:** запуск рабочего процесса по шаблону Toyota (карточка `TPS-0001`, поток `backlog → … → done` один раз).
**Хансей (反思)** в тойотовской традиции — самокритика по итогам цикла: сначала то, что было подано лучше, чем оно есть, потом вывод для процесса. Этот документ не отчёт о проделанном: отчёт — в `board.json` и `ACTIVE_TASKS.md`.

## 1. Что было заявлено раньше, чем следовало

- **Единственная «зелёная» проверка процесса, которую я сам же написал, — не верификация.** Если бы цикл закончился на «стандарт описан, доска заведена, гейт зелёный», результат был бы ТУФТОЙ по RCVAP §2 («отчёт о промежуточном артефакте как о прогрессе»). Стоп-линия сработала корректно только потому, что гейт **упал** на реальной находке (битая ссылка и четыре несуществующих каталога), а не потому, что он красивый.
- **Искусительная подмена в этом цикле — надуть доску.** Восемь карточек на доске — ровно восемь: один выполненный цикл, два конкретных открытых пункта реестров (`F-08`, хейдзанка-пересказ) и пять ожидающих решения владельца или внешний DOI. Соблазн «показать процесс» был велик: доска принимает десятки карточек, и ни одна проверка не отличила бы выдуманную задачу от настоящей. Отличие держится на правиле `CARD_SOURCE_MISSING` (каждая карточка обязана ссылаться на существующий файл-основание) — оно и есть единственная реальная узда, и это надо помнить: **доску нельзя наполнять «из головы»**.
- **Тактовые замеры — слабое место цикла.** `takt.samples` заполнены вручную (n=1) из логов разных прогонов, часть — до финальной записи доски. Честная формулировка: это *замеры времени команд этого сеанса*, а не медиана процесса. Предложено как `K-0003` (автоматическое снятие длительностей) и не считается закрытым улучшением.

## 2. Найденное в себе (по фактам)

| Наблюдение | Основание | Следствие |
|---|---|---|
| Порядок проверок в собственном валидаторе имел дыру: `DONE_NO_VERIFICATION` возвращался раньше, чем проверялся маркер аудитора | Мутационный тест «even with no commands at all» показал, что правило не срабатывает | Проверки порядка зависимых инвариантов вынесены вперёд; правило K-0002 (страж обязан иметь мутационный тест) подтверждено фактом, а не декларацией |
| Первый прогон гейта поймал недостающий `tps/BOARD.md` — до того, как я успел его сгенерировать | Лог `npm run tps:board`: `DOC_LINK_BROKEN x1` | Проверка работает как ограничение, а не как рекомендация — главный критерий полезности слоя |
| Открытый андон `A-0003` (medium) сначала хотелось «закрыть для чистоты доски» | Он зависит от решений владельца (F-05/F-09…F-11), которых у исполнителя нет | Оставлен открытым; валидатор требует для закрытия перепроверку `exit 0` — закрытие без неё невозможно по построению |
| Я потратил цикл на исправление чужих опечаток в двух документах, хотя цель была «запустить процесс» | `DEFERRED_LOCALIZATION_TASK.md`, `DOCUMENTATION_CATALOG.md` | Это не расширение скоупа, а часть P-11: процесс без устранённого корня первой находки был бы латанием симптома; правка — только исправление битого пути и честная маркировка несуществующих каталогов; ни один статус доказательств не изменён |

## 2.1 Два отказа, которые процесс поймал на самом себе (не «外部环境», а мой брак)

- **Полный прогон на незакоммиченном дереве (`exit 1`, 1 падение `OIR03-QA-36`).** Первая реакция — «тест мешает» и соблазн дописать свои пути в allowlist верификатора. Это была бы ТУФТА по §2 RCVAP. Контрмера: коммитить годное состояние и проверять на нём (андон `A-0004`), а потерянные 209 с записать как потерю `M-0005`.
- **Прогон на чистом коммите: 1966/1975 — красные 9 тестов, и все мои.** Вот здесь хансей по-настоящему неприятен: я написал страж, мутационные тесты которого заимствовали эталон из живой доски. Когда я сам законно переместил карточку `done → verify` (реакция на `A-0004`), девять правил проверки **молча перестали что-либо ловить**. Красные тесты — меньшее из зол: зелёные-и-бессильные — вот настоящий дефект контроля. Закрыто построением эталона кодом теста + отдельным тестом «эталон зелёный до мутации» (`A-0006`, `K-0007`).
- **Отдельно о `.gitignore`.** Строка, затемняющая путь к workflow ядровой проверки, не мешала никому и ничему — ровно до момента, когда файл снова потеряют. Тойотовский вопрос здесь не «падает ли сейчас», а «что произойдёт при следующем дефекте»: `git add -A` проигнорировал бы восстановление (андон `A-0005`, high).

## 2.2 Такт 2: дубль закрытой работы (самый неприятный раздел этого файла)

Второй такт потянул в работу `F-08`, написал кандидат-ремонт (три тактические подстановки, перегенерация производных, локальные стражи, PR #49) — и только потом выяснилось, что `main` уже закрыл этот дефект PR #47 с **фактическим прогоном ядра** (`run 35024728967`, `sorryAx-free`, A11 → `LEAN_VERIFIED`), а «моя» карточка `PENDING_DOI` закрыта PR #48. Оба случая — перепроизводство по Тойоте, и оно не нейтрально: второй непроверенный «ремонт» рядом с проверенным ухудшает доказательную базу, то есть бьёт по самой ценности проекта.

Что важно в формулировке: я не «потратил время» — я произвёл **альтернативную версию факта** и был готов открыть её как результат. Причина не в невезении с параллельной сессией, а в том, что процесс ограничивал количество работ, но не проверял их актуальность. Отсюда меры: пункт 6 тяги (`git fetch` + перечитывание реестра перед началом), правила `CARD_FINDING_ALREADY_CLOSED`/`CARD_SOURCE_MISSING` (они поймали обе мои карточки до того, как я их закрыл — и заодно мой же неверный cycle time), и отдельная семантика `closure` для работы, сделанной вне потока, чтобы честно закрывать чужое закрытие, не присваивая его себе.

Остаточная честная оговорка: распознавание «закрыто» опирается на словарь префиксов в тексте `resolution` реестра. Это слабее явного поля `status`, и предложение `K-0011` (перевести закрытость в машиночитаемое поле реестра) остаётся открытым — оно требует решения владельца о формате нормативного артефакта, а не моей инициативы.

## 2.3 Такт 3: слепота собственного правила (и почему хансей без этого раздела был бы декорацией)

Третий такт делал генерируемую сводку находок (`FINDINGS_DIGEST.md`) — формально небольшую работу. По дороге выяснилось, что **правило, написанное мной же 30 минут назад**, читало закрытость находки только из `resolution`, тогда как у половины находок (F-09…F-14) она лежит в `status`; сравнение предикатов на реальном реестре дало ровно одно расхождение — F-12 («ЗАКРЫТО ФАКТИЧЕСКИМ ПРОГОНОМ») не виделось вовсе. Гейт оставался зелёным и при этом ничего не видел на половине данных: это тот случай, когда «страж, который нельзя покраснить, — не доказательство», только покраснить его было нечем.

Отдельно зафиксировано, *почему* тест это пропустил: мутационный тест строился на F-08 — на той же форме данных, из которой правило было выведено. Проверка «срабатывает ли правило на примере, из которого его написали» — самоциклирование верификатора; отсюда тест на перебор всех находок и тест на форму «Частично закрыто».

Второй неприятный момент такта: сводка честно показала, что `F-01` формально «закрыто» по маркеру «Решение владельца», хотя уровень узла `registry-120` всё ещё ждёт решения L9. Правило не врёт — оно механическое; для частичных закрытий существует явное `findingReferenceWaivers`, и потому оно требует указывать остаток, а не слово «ещё не закрыто». Соблазн расширить словарь префиксов «под каждую встречу» не реализован: это превратило бы проверку в гонку за текстом, и вместо него оставлено `K-0011` (машиночитаемое поле закрытия в реестре) — как решение владельца о формате нормативного артефакта.

## 3. Изменения процесса, принятые по итогам такта

1. `P-11` введён в `WORK_PATTERNS.md` — поток стал переиспользуемой процедурой, а не разовым усилием (механизм P-10: следующий цикл ссылается, а не повторяет).
2. `tps:gate` и `tps:board:check` — шаг CI в `pr-verify.yml`: нарушения процесса ломают PR, а не живут в надежде на ревью.
3. `K-0007` (DONE) и `K-0002` (ACCEPTED): мутационный тест обязан иметь валидный сконструированный базис: любое новое правило валидатора приходит только вместе с мутационным тестом (иначе правило не считается внедрённым).

## 4. Что взять следующему такту

- После каждого изменения любого `tools/*.test.ts` — локальный прогон этого guard-файла до коммита (`M-0006`), полная проверка — на годном состоянии.
- Начинать с `npm run tps:gate` (иначе тянуть нельзя), и с `in_progress` ровно одной карточки: `TPS-0002` (F-08) — она в `ready` и не блокируется андоном.
- Проверить на себе хейдзанку: второй такт не должен быть снова `math-proof` подряд с `docs` — `maxPerClass` это ловит, но только если очередь не наполнять задним числом.
- Закрыть `A-0003` можно **только** решением владельца; попытка закрыть его зелёным прогоном формы — пример ТУФТЫ, на который этот гейт не влияет и который должен заметить человек.

## 5. Оценка цикла (по тойотовской традиции — без похвалы)

| Метрика | Значение | Комментарий |
|---|---|---|
| Cycle time карточки `TPS-0001` | ~18.7 ч от первой команды цикла до финального прогона (разность зафиксированных таймстампов; `CYCLE_TIME_MISMATCH` не даёт заявлять другое) | Время включает ожидание длинных прогонов (209 с + 213 с) и реальный перерыв инструмента; это не «продуктивный такт», и выдавать его за такой было бы ТУФТОЙ |
| Открытых блокирующих андонов | 0 (зарегистрировано 8: A-0001…A-0008; открыты 2 — A-0003 и A-0007, оба по причине вне зоны исполнителя) | Не «всё хорошо», а «высоких находок не осталось»; средний `A-0003` открыт |
| Потери (muda) в реестре | 6 записей | Все четыре — не украшение таблицы: `waiting`×1, `overprocessing`×1, `defects`×1 (со ссылкой на андон), `inventory`×1 |
| Доля карточек, требующих решения владельца | 4 из 8 | Главная узкая горловина потока — не исполнительность, а решения по классу заявлений |

---

**AUDITOR: SELF (same-pipeline)** — хансей написан исполнителем того же цикла; внешний рецензент процесса не привлекался, и независимой оценки «улучшился ли процесс» не существует.
